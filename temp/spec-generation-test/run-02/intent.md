# Feature Intent

We need a CLI orchestrator (**`axm`**) that turns loose raw work input into contract-bound workflow artifacts using AxiomMd skills, performs validation per `workflow_check.py`, and produces clear human handoffs.

This run demonstrates PRD-to-package generation quality for a complex, UI-augmented, non-trivial orchestrator.
