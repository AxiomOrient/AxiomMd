# Spec Input Packet

Use this worksheet only when the target repo does not already own a normalized packet format.

For AxiomMd/AxiomSpecs work, the authoritative normalized input is AxiomMd `templates/input.packet.yaml`.
This file is a fallback worksheet for non-Axiom repos or ad hoc spec work.

## 1. Task Summary

- Requested outcome:
- Primary operator or user:
- Why this matters now:
- Intended artifact: `full-spec` | `scoped-artifact` | `unknown`

## 2. Scope Boundaries and Non-Goals

- In scope:
- Out of scope:
- Non-goals:
- Changes that need approval before execution:

## 3. Current Reality Evidence

### 3.1 Source-of-Truth Files and Paths

- `[path or document]` — why it matters

### 3.2 Observed Current Behavior

- What the system does today:

### 3.3 Current Commands, Tests, Logs, or Failures

- `command` — what it shows
- Concrete failure, warning, or missing evidence:

## 4. Target Delta

- What must change:
- What must stay stable:
- External contracts, invariants, or compatibility rules that cannot break:

## 5. Definition of Done

- Validation commands:
- Expected observable signals:
- Manual review or release checks:

## 6. Precision Boosters

- Sample input or output:
- Edge cases and negative cases:
- Lifecycle or state transition examples:
- Migration or rollback expectations:
- Performance, security, or compliance constraints:
- Stable glossary or naming rules:

## 7. Open Questions and Safe Assumptions

- `OPEN:` unknown fact, why it matters, and when it must be decided
- Assumption: narrow, reversible choice made to keep drafting moving

## 8. Artifact Recommendation

- Recommend: `full 18-section spec` | `scoped artifact`
- Reason:
- If scoped, list omitted or `N/A` standard areas:
